test:
  override:
    - echo "test"
    
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync infrastructure s3://diginsur-refarch/infrastructure/ --delete
      - aws s3 sync master.yaml s3://diginsur-refarch/master.yaml --delete