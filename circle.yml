test:
  override:
    - echo "test"

deployment:
  prod:
    branch: master
    commands:
      - aws s3 mb s3://${CIRCLE_BUILD_NUM}
      - aws s3 sync infrastructure s3://${CIRCLE_BUILD_NUM}/infrastructure/ --delete
      - aws s3 cp ./master.yaml s3://${CIRCLE_BUILD_NUM}/

#      - aws s3 rb s3://$CIRCLE_BUILD_NUM